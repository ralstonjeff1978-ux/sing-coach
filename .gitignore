.venv/
__pycache__/
*.pyc
*.pyo

# Downloaded models (~65 MB each) — fetched on first run, never committed.
models/

# Per-song derived audio and analysis. Regenerable from the source files.
cache/

# Session history and user settings.
data/

# Never commit audio.
*.mp3
*.mp4
*.m4a
*.wav
*.flac
*.ogg
*.opus

.pytest_cache/
